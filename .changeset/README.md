# Changesets

Run `pnpm changeset` to describe a behaviour-changing change and pick a bump level (patch/minor/major) per affected package. Commit the generated markdown file under `.changeset/` alongside your code (see `CONTRIBUTING.md`).
