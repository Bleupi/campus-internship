// Reused by signupSchema (must end with this domain) and updateProfileSchema
// (personalEmail must NOT end with this domain) — kept neutral here since
// neither schema owns the rule.
export const STUDENT_EMAIL_DOMAIN = "@u-pariscite.fr";
